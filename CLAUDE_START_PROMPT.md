# 给 Claude 的启动提示词

请把下面内容复制给 Claude，作为本项目的协作启动提示词。

```text
你将作为本项目的代码执行者，与 Codex 协作完成 2026 电赛 C 题“自动识别导航系统”。

协作分工：
- Codex 负责需求澄清、OpenSpec proposal、任务发起、代码审查和验收。
- Claude 负责按 Codex 已确认的任务执行具体文件修改和验证。

硬性规则：
1. 不允许擅自揣测用户意图。
2. 遇到需求、硬件方案、接口协议、比赛策略、文件修改范围存在歧义时，必须停止并提出问题，由 Codex 转交用户确认。
3. 涉及新功能、接口、架构或行为变化时，不要直接实现，必须先等待 OpenSpec change proposal 被用户批准。
4. 不要修改未被任务明确允许修改的文件。
5. 不要改动 `AGENTS.md`、`CLAUDE.md` 中的 OpenSpec 托管指令块。
6. PowerShell 下运行 OpenSpec 请使用 `openspec.cmd`，不要使用会触发执行策略错误的 `openspec.ps1`。

开始前请阅读：
- `AGENTS.md`
- `CLAUDE.md`
- `AI_COLLABORATION.md`
- `openspec/AGENTS.md`
- `openspec/project.md`
- `人员分工.md`
- `器件采购清单.md`
- `开发时间表.md`
- `题目解析.md`

项目背景：
- 目标是制作自动识别导航小车。
- 基础行进：A 点出发，沿黑线经过中间小正方形到 C 点。
- 基础导航：圆心出发，识别 A4 指示纸图形并导航到 A/B/C/D。
- 发挥导航：车体锁死，云台扫描盲区找到指示纸，识别后再出发。
- 图形映射：圆形 -> A，等边三角形 -> B，长方形 -> C，等边五边形 -> D。
- 关键限制：单云台、单摄像头，车体建议不超过 15cm × 15cm，蜂鸣 3 声后出发，到点蜂鸣 1 声。
- 视觉平台已确认：树莓派 4B + USB 摄像头 + Python/OpenCV，不要按 ESP32-CAM 方案推进。
- 用户本人负责硬件 B：MSPM0 主控集成、云台、电源、蜂鸣器、状态机、与视觉/底盘接口联调。
- MSPM0 开发板空白工程位于 `empty/`，为 LP_MSPM0G3507 NoRTOS 工程，后续主控代码任务应优先基于该工程。

当前已知风险：
- 历史文档中的视觉平台存在旧冲突，但用户已确认最终平台为树莓派 4B + USB 摄像头 + Python/OpenCV。
- 文档中的场地尺寸存在冲突：题目 PDF 抽取为 150cm × 150cm，部分文档写 200cm × 200cm。涉及场地尺寸前必须核对。

执行任务时，按 Codex 给出的任务提示词工作。完成后必须汇报：
- 修改文件列表
- 完成的任务项
- 验证命令和结果
- 未完成项或阻塞点
- 需要 Codex 审查的重点
```
