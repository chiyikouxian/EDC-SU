# 调车测试文档

> 主工程：`C:\Users\ideapad15s\workspace_ccstheia\EDC-SU`
> 参考/供体工程：`C:\Users\ideapad15s\workspace_ccstheia\EDC-SHEN-Car`
> 阶段目标：真实底盘实车运动调参
> 编写日期：2026-05-07

## 1. 测试目标

Phase 1 已完成真实底盘迁移和主工程串口验收。Phase 2 的目标不是继续改接口，而是在现有接口合同冻结的前提下完成实车运动调参：

- 确认四轮方向、基础运动、刹车和锁电机可靠。
- 确认循迹修正方向正确。
- 调整底盘速度参数，使车辆能稳定低速跑完整线。
- 调整 PID 参数，使车辆减少摆动、过冲和出线。
- 验证节点识别、终点到达、丢线保护和错误保护。
- 最后验证 DRV / NAV / ADV 完整任务流程。

## 2. 冻结边界

本阶段不要修改以下接口和语义：

- `gray_sensor.c/h`
- `line_track.c/h`
- `track_bridge.c/h`
- `app_state` 中 `RUNNING -> track_bridge_update() -> chassis_tick()` 的顺序
- `chassis_get_status()` 合同
- `ChassisStatus_t` 枚举
- `track_bridge` stale 语义
- `main.c` 中 `motor_update_pwm()` 1ms、`app_state_tick()` 10ms 的调度关系
- encoder 相关配置，保持 `CHASSIS_USE_ENCODER = 0`

允许调整的最小范围：

- 电机方向错误时，只改 `motor.c` 中 `MOTOR_A_POLARITY`、`MOTOR_B_POLARITY`、`MOTOR_C_POLARITY`、`MOTOR_D_POLARITY`。
- 运动参数只改 `chassis_iface.c` 中的速度、PID、节点阈值等底盘侧参数。
- 如需记录测试结论，可更新本文档或另写测试记录，不改接口合同。

## 3. 当前基线参数

当前主工程关键参数如下，调车前先记录一遍实际代码值：

```c
#define CHASSIS_BASE_SPEED                  22
#define CHASSIS_MAX_SPEED                   45
#define CHASSIS_CURVE_SPEED                 18
#define CHASSIS_FIND_SPEED                  16
#define CHASSIS_CROSS_CONFIRM                4
#define CHASSIS_LOST_LINE_SHORT_TICKS        6
#define CHASSIS_LOST_LINE_LONG_TICKS       150
#define CHASSIS_CURVE_ERROR                420
#define CHASSIS_TRACK_ERROR_SCALE            7
#define CHASSIS_NODE_ACTIVE_COUNT_THRESHOLD  6
#define CHASSIS_USE_ENCODER                  0
```

当前 PID 初始化：

```c
pid_init(&line_pid, 18, 0, 10, 38, 1600);
```

含义：

- `kp = 18`
- `ki = 0`
- `kd = 10`
- 输出限幅 `38`
- 积分限幅 `1600`

当前电机极性：

```c
#define MOTOR_A_POLARITY  1
#define MOTOR_B_POLARITY -1
#define MOTOR_C_POLARITY -1
#define MOTOR_D_POLARITY  1
```

四路映射：

- A：左前
- B：左后
- C：右后
- D：右前

`motor_set_speed(left, right)` 当前最终等价于：

```c
motor_set_wheels(left, left, right, right);
```

## 4. 串口观察字段

主工程 DEBUG_UART 输出格式：

```text
S=... M=... T=... C=... G=... L=... B=...
```

字段含义：

- `S=`：主控 app_state，例如 `IDLE`、`SBEEP`、`RUN`、`TSTOP`、`FBEEP`、`ERR`
- `M=`：运行模式，例如 `DRV`、`NAV`、`ADV`
- `T=`：目标点，例如 `A`、`B`、`C`、`D`、`-`
- `C=`：底盘状态，例如 `IDL`、`FLIN`、`FOLL`、`RCHD`、`LOST`、`ERR`
- `G=`：8 路灰度传感器原始状态
- `L=`：line_track 计算结果
- `B=`：track_bridge 输出给底盘的数据

重点验收：

- IDLE / SBEEP：`B=STALE` 是正常现象。
- RUN 正常循迹：`C=FOLL`，`B=E:...` 随车身偏移变化。
- 短时丢线：`C=FLIN`。
- 长时丢线：`C=LOST`，随后主控进入 `S=ERR`。
- 到达目标节点：`C=RCHD`，随后退出 `RUN`。
- stale 错误：`C=ERR`，随后主控进入 `S=ERR`。

## 5. 测试前准备

硬件准备：

- 车体固定可靠，电池电量充足。
- 四轮离地支架或架空条件可用。
- 测试赛道具备直线、弯道、节点/交叉区域。
- 串口监视器连接 DEBUG_UART，波特率按主工程配置使用。
- 准备紧急断电手段。

软件准备：

- 使用主工程当前 Phase 1 验收通过版本。
- 确认未打开 encoder：`CHASSIS_USE_ENCODER == 0`。
- 确认未改 `gray_sensor`、`line_track`、`track_bridge` 合同。
- 每次改参数后记录：参数值、现象、是否通过。

安全要求：

- 第一次测电机方向必须架空车轮。
- 车轮方向未确认前不要落地跑线。
- 出现冲刺、反向修正、刹不住、异常发热时立即断电。
- 每次只改一个变量，避免无法定位原因。

## 6. 第二阶段：基础运动测试

### 6.1 四轮方向架空测试

目的：确认电机极性和四轮物理方向。

步骤：

1. 将车架空，四轮离地。
2. 进入可触发底盘输出的测试状态。
3. 低速触发前进。
4. 观察 A/B/C/D 四个轮子方向。
5. 记录每个轮子是否产生车体前进方向。

通过标准：

- A 左前、B 左后、C 右后、D 右前全部方向正确。
- 四轮没有某一路不转、反转、明显抖动。
- 停止后电机输出能关闭或刹住。

处理规则：

- 单个轮方向反：只改对应 `MOTOR_*_POLARITY`。
- 左侧两轮和右侧两轮都反：优先检查左右侧物理接线和极性。
- 不要为了方向问题修改 `line_track`、`track_bridge` 或 PID。

记录表：

| 项目 | 现象 | 结论 | 修改 |
| --- | --- | --- | --- |
| A 左前 |  |  |  |
| B 左后 |  |  |  |
| C 右后 |  |  |  |
| D 右前 |  |  |  |

### 6.2 低速直行测试

目的：确认 `chassis_forward` / `motor_set_speed(left,right)` 的左右侧映射正确。

步骤：

1. 低速落地，留出安全距离。
2. 触发低速直行。
3. 观察车体是否大致直行。
4. 停车，检查刹车/滑行是否符合预期。

通过标准：

- 车体向前运动。
- 不出现原地转圈。
- 轻微偏航可以接受，明显偏航需记录。

处理规则：

- 如果前进变后退，优先检查四轮极性。
- 如果变原地旋转，优先检查左右侧映射。
- 如果只是轻微偏航，先不要改接口，进入后续 PID/速度调试再判断。

### 6.3 原地旋转测试

目的：确认 `chassis_rotate_left()` 和 `chassis_rotate_right()` 方向正确。

步骤：

1. 低速触发左旋。
2. 观察车体是否逆时针旋转。
3. 停车。
4. 低速触发右旋。
5. 观察车体是否顺时针旋转。

通过标准：

- 左旋、右旋方向符合预期。
- 旋转过程中左右侧车轮方向相反。
- 不出现某一侧不转或打滑严重。

处理规则：

- 旋转方向反，优先检查 `chassis_drive(forward, turn)` 到左右轮的符号关系和电机侧映射。
- 仅某个轮异常，优先回到四轮极性测试。

### 6.4 刹车和锁电机测试

目的：确认异常保护可真正停止车体。

步骤：

1. 低速前进。
2. 触发停止、刹车或进入错误保护。
3. 观察车轮是否停止。
4. 观察串口状态是否进入对应状态。

通过标准：

- 停止动作及时。
- `S=ERR` 时电机应锁止或保持安全状态。
- 不出现错误态下继续驱动。

## 7. 第三阶段：低速循迹方向测试

### 7.1 error 符号确认

已验证合同：

- X1 左侧，X8 右侧。
- line_track 输出 `[-100,+100]`。
- 左偏为负，右偏为正。

步骤：

1. 让车进入 `RUN`，但先用低速参数。
2. 手动将车体相对黑线向左偏。
3. 观察 `B=E:负值`。
4. 手动将车体相对黑线向右偏。
5. 观察 `B=E:正值`。

通过标准：

- 左偏时 `B=E:` 为负。
- 右偏时 `B=E:` 为正。
- 中线附近 error 接近 0。

如果不通过：

- 暂停调车。
- 不要改 PID。
- 回查灰度排布、传感器方向、line_track 合同，但当前阶段原则上不改这些模块。

### 7.2 修正方向确认

目的：确认 PID 输出作用到左右轮后，车体修正方向正确。

步骤：

1. 车体低速进入 `RUN`。
2. 让车轻微偏左，观察车是否向右修。
3. 让车轻微偏右，观察车是否向左修。
4. 观察 `C=` 是否保持 `FOLL` 或短暂 `FLIN`。

通过标准：

- 偏左向右修。
- 偏右向左修。
- 修正动作不应反向加剧偏差。

处理规则：

- 如果修正方向反了，优先检查左右电机映射或 `left = base + correction; right = base - correction;` 方向关系。
- 不要通过反转 `track_bridge` error 语义解决。

## 8. 第四阶段：速度参数调试

调试原则：

- 先稳定，后速度。
- 每次只改一个速度参数。
- 每组参数至少跑 3 次直线和 3 次弯道。

### 8.1 `CHASSIS_BASE_SPEED`

作用：正常循迹基础速度。

当前值：`22`

建议范围：

- 初次落地保守值：`16~18`
- 稳定后常用值：`20~24`
- 不建议第一轮超过：`28`

现象判断：

| 现象 | 调整 |
| --- | --- |
| 起步太猛、冲线 | 降低 `BASE` |
| 直线能跑但弯道冲出 | 先降低 `BASE`，再看 `CURVE` |
| 车体带不动或频繁停滞 | 小幅提高 `BASE` |
| 低速稳定，高速摆动 | 先降低 `BASE`，不要先大改 PID |

### 8.2 `CHASSIS_FIND_SPEED`

作用：短时丢线后找线旋转速度。

当前值：`16`

建议范围：

- 初次测试：`12~14`
- 稳定后：`14~18`

现象判断：

| 现象 | 调整 |
| --- | --- |
| `FLIN` 后旋转太猛，越找越远 | 降低 `FIND` |
| 丢线后找线太慢 | 小幅提高 `FIND` |
| 找线方向反 | 检查 `last_line_error` 和旋转方向，不先改速度 |

### 8.3 `CHASSIS_CURVE_SPEED`

作用：误差较大或弯道状态下的基础速度。

当前值：`18`

建议范围：

- 初次测试：`14~16`
- 稳定后：`16~20`

现象判断：

| 现象 | 调整 |
| --- | --- |
| 入弯冲出 | 降低 `CURVE` |
| 弯道过慢但稳定 | 小幅提高 `CURVE` |
| 弯中左右摆动 | 先看 PID，不只靠降速度 |

## 9. 第五阶段：PID 调参

调参顺序：

1. `CHASSIS_TRACK_ERROR_SCALE`
2. `kp`
3. `kd`
4. `ki` 暂时保持 0

### 9.1 `CHASSIS_TRACK_ERROR_SCALE`

作用：将 bridge error `[-100,+100]` 放大到底盘 PID 输入。

当前值：`7`

建议范围：

- 保守：`4~5`
- 当前基线：`7`
- 激进：`8~10`

现象判断：

| 现象 | 调整 |
| --- | --- |
| 小偏差时车不修或修得慢 | 提高 `SCALE` |
| 一偏就猛打方向 | 降低 `SCALE` |
| 左右快速摆动 | 降低 `SCALE` 或降低 `kp` |

### 9.2 `kp`

作用：主要修正力度。

当前值：`18`

建议调整步进：每次 `2~3`

现象判断：

| 现象 | 调整 |
| --- | --- |
| 入弯反应慢 | 提高 `kp` |
| 直线小幅蛇形 | 降低 `kp` 或提高 `kd` |
| 大幅左右摆动 | 降低 `kp` |

### 9.3 `kd`

作用：抑制快速变化和过冲。

当前值：`10`

建议调整步进：每次 `2~4`

现象判断：

| 现象 | 调整 |
| --- | --- |
| 修正后过冲明显 | 提高 `kd` |
| 动作发抖、响应不连续 | 降低 `kd` |
| 弯道响应慢 | 不要只加 `kd`，优先看 `kp` 和 `SCALE` |

### 9.4 `ki`

当前值：`0`

本阶段默认不启用积分。只有在确认机械偏差、左右电机差异、速度参数、`kp/kd` 都较稳定后，仍存在长期固定偏差，才考虑非常小的 `ki`。第一轮实车调参不建议开启。

## 10. 第六阶段：节点和终点识别测试

### 10.1 节点识别

相关参数：

```c
#define CHASSIS_NODE_ACTIVE_COUNT_THRESHOLD 6
#define CHASSIS_CROSS_CONFIRM              4
```

含义：

- `active_count >= 6` 视为进入节点/交叉区域。
- 连续确认 4 个 tick 后计入节点。
- tick 当前为 10ms，因此确认时间约 40ms。

步骤：

1. 低速跑过节点区域。
2. 观察串口 `CH:` 调试输出中的 `active`、`node`。
3. 重复从不同角度进入节点。
4. 记录误判和漏判次数。

通过标准：

- 正常节点能稳定计数。
- 普通弯道或宽线区域不应频繁误判节点。
- 到达目标节点数后 `C=RCHD`。

调整建议：

| 现象 | 调整 |
| --- | --- |
| 普通线也误判节点 | 提高 `CHASSIS_NODE_ACTIVE_COUNT_THRESHOLD` 或提高确认要求 |
| 真实节点漏判 | 降低 `CHASSIS_NODE_ACTIVE_COUNT_THRESHOLD` |
| 节点处重复计数 | 检查 `cross_latched` 效果和车速，不先改合同 |

### 10.2 目标节点数

当前目标节点表：

```c
static const int target_finish_nodes[] = {
    1,  /* Target A */
    1,  /* Target B */
    2,  /* Target C */
    1   /* Target D */
};
```

步骤：

1. 分别测试 A/B/C/D。
2. 记录实际应该经过的节点数。
3. 对比 `target_finish_nodes[]`。
4. 确认到达后 `C=RCHD`，随后 `S=TSTOP -> FBEEP -> IDLE`。

通过标准：

- 每个目标都在正确节点退出 RUN。
- 不提前 RCHD。
- 不越过终点后继续跑。

## 11. 第七阶段：异常保护测试

### 11.1 短时丢线

目的：验证短时丢线进入找线状态。

步骤：

1. 车辆稳定 `C=FOLL`。
2. 短时间移开黑线或让车经过短断线区域。
3. 观察 `C=`。

通过标准：

- 约 60ms 后进入 `C=FLIN`。
- 车体按上一次 error 方向进行找线。
- 找回线后恢复 `C=FOLL`。

### 11.2 长时丢线

目的：验证长时丢线保护。

步骤：

1. 车辆稳定 `C=FOLL`。
2. 长时间移开黑线或抬离赛道。
3. 观察串口和电机动作。

通过标准：

- 约 1500ms 后进入 `C=LOST`。
- 随后主控进入 `S=ERR`。
- 电机停止并锁定安全状态。

### 11.3 stale 错误

目的：验证 bridge 数据失效时底盘不会继续使用旧数据。

说明：该项通常需要临时注入或复用已有 stale 验证方式。测试后必须恢复代码。

通过标准：

- `B=STALE`。
- `C=ERR`。
- 随后 `S=ERR`。
- 电机刹车/锁定。

## 12. 第八阶段：完整任务流程测试

### 12.1 DRV 模式

目的：验证固定目标 C 的完整跑线流程。

步骤：

1. 切到 `DRV`。
2. 按启动键。
3. 等待启动蜂鸣。
4. 车辆进入 RUN。
5. 跑到目标 C。

通过标准：

```text
IDLE -> SBEEP -> RUN -> RCHD -> TSTOP -> FBEEP -> IDLE
```

串口应满足：

- `M=DRV`
- `T=C`
- RUN 中 `B=` fresh
- 正常循迹 `C=FOLL`
- 到达后 `C=RCHD`

### 12.2 NAV 模式

目的：验证视觉给目标后的固定检测流程。

步骤：

1. 切到 `NAV`。
2. 按启动键。
3. 发送或等待视觉目标，例如目标 C。
4. 车辆启动并完成跑线。

通过标准：

```text
IDLE -> FDET -> SBEEP -> RUN -> RCHD -> TSTOP -> FBEEP -> IDLE
```

串口应满足：

- `M=NAV`
- `T=` 与视觉目标一致
- RUN 中 `B=` fresh
- 到达后 `C=RCHD`

### 12.3 ADV 模式

目的：验证云台扫描、视觉识别、底盘跑线完整流程。

步骤：

1. 切到 `ADV`。
2. 按启动键。
3. 云台开始扫描。
4. 视觉识别目标。
5. 云台停止，车辆启动跑线。

通过标准：

```text
IDLE -> GSCAN -> SBEEP -> RUN -> RCHD -> TSTOP -> FBEEP -> IDLE
```

串口应满足：

- `M=ADV`
- `T=` 与视觉目标一致
- RUN 中 `B=` fresh
- 到达后 `C=RCHD`

## 13. 第九阶段：重复性和抗干扰测试

目的：确认参数不是只对单次测试有效。

测试项目：

- DRV 同一目标连续跑 5 次。
- NAV 不同目标各跑 3 次。
- ADV 完整流程至少跑 3 次。
- 满电、中等电量、低电量各测试一次。
- 起点左偏、右偏、前后偏移各测试一次。
- 光照变化测试。
- 节点不同进入角度测试。

通过标准：

- 无随机冲出。
- 无频繁误判节点。
- 无异常进入 `S=ERR`。
- 失败模式可复现、可解释、可通过参数调整改善。

## 14. 第十阶段：encoder 闭环预留测试

本阶段暂不执行 encoder 闭环。只有 Phase 2 实车开环调参完成后，才进入 encoder 测试。

进入条件：

- 四轮方向和基础运动已稳定。
- 开环循迹能稳定跑完整路线。
- 节点和异常保护已通过。
- encoder 接线确认无误。

后续测试内容：

- syscfg 增加 `GPIO_ENCODER`。
- 打开 `CHASSIS_USE_ENCODER=1`。
- 验证四路计数方向。
- 验证左右速度闭环。
- 重新调速度和 PID。

## 15. 参数记录模板

每次调整后记录一行：

| 日期 | 测试人 | BASE | CURVE | FIND | SCALE | kp | ki | kd | 节点阈值 | 现象 | 结论 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
|  |  | 22 | 18 | 16 | 7 | 18 | 0 | 10 | 6 |  |  |

## 16. 问题记录模板

| 编号 | 阶段 | 现象 | 串口输出 | 复现步骤 | 初步判断 | 处理动作 | 结果 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P2-001 |  |  |  |  |  |  |  |

## 17. 最小执行顺序

硬件 A 实际调车时按以下顺序执行，不要跳步：

1. 四轮方向架空测试。
2. 低速直行、左旋、右旋、刹车测试。
3. 低速循迹 error 符号和修正方向测试。
4. 调 `CHASSIS_BASE_SPEED`、`CHASSIS_FIND_SPEED`、`CHASSIS_CURVE_SPEED`。
5. 调 `CHASSIS_TRACK_ERROR_SCALE`、`kp`、`kd`。
6. 验证节点识别和 `RCHD`。
7. 验证 `FLIN`、`LOST`、`ERR`。
8. 验证 DRV 完整流程。
9. 验证 NAV 完整流程。
10. 验证 ADV 完整流程。
11. 做重复性和抗干扰测试。
12. 全部稳定后，再讨论 encoder Phase 2+。

## 18. 交付标准

硬件 A 完成调车后，应交付：

- 最终参数表。
- 每个阶段的通过/失败记录。
- 至少一组 DRV 完整跑线串口记录。
- 至少一组 NAV 完整跑线串口记录。
- 至少一组 ADV 完整跑线串口记录。
- 所有已知失败模式和复现步骤。
- 如改过 `motor.c` 极性，说明每一路修改原因。
- 如改过 `chassis_iface.c` 参数，说明每个参数修改前后现象。

最终验收条件：

- DRV / NAV / ADV 能完成目标流程。
- `FOLL / FLIN / LOST / RCHD / ERR` 状态符合实车现象。
- 异常情况下电机能停止并进入安全状态。
- 未修改冻结接口合同。
- encoder 仍保持关闭，留到后续阶段。
