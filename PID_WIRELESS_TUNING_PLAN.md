# PID 无线串口分阶段优化计划

## 1. 目标与原则

本计划依次完成：无线遥测链路验证、编码器方向检查、开环速度标定、左右轮速度 PI、循迹 PD、整车联合优化。

执行时遵守以下原则：

- 一次只修改一个参数。
- 每组参数至少完成 3 次相同条件测试。
- 先架空、后落地；先直线、后弯道；先低速、后提速。
- 每次改参数前记录当前值，异常时立即恢复上一组稳定参数。
- 速度环没有稳定前，不调整循迹环。
- 测试时保留机械急停条件，车辆失控立即断开电机电源。

## 2. 当前基线

### 2.1 速度环

| 参数 | 当前值 |
|---|---:|
| `CHASSIS_USE_ENCODER` | 1 |
| `CHASSIS_USE_SPEED_PID` | 1 |
| `CHASSIS_SPEED_COUNTS_PER_CMD_X100` | 42 |
| `CHASSIS_SPEED_PID_KP` | 2 |
| `CHASSIS_SPEED_PID_KI` | 1 |
| `CHASSIS_SPEED_PID_KD` | 0 |
| `CHASSIS_SPEED_PID_CORRECTION_LIMIT` | 8 |
| `CHASSIS_SPEED_PID_INTEGRAL_LIMIT` | 1200 |

### 2.2 循迹环

| 参数 | 当前值 |
|---|---:|
| `Kp` | 24 |
| `Ki` | 0 |
| `Kd` | 10 |
| 输出限幅 `CHASSIS_TURN_LIMIT` | ±6 |
| `CHASSIS_TRACK_ERROR_SCALE` | 1 |

### 2.3 无线串口

- 主控接口：UART0，`PA10=TX`、`PA11=RX`。
- 当前波特率：115200，8N1，无流控。
- 无线模块应工作在透明传输模式，并与主控、电脑接收端保持相同波特率。
- 遥测格式：`P,t,tl,tr,ml,mr,el,er,pl,pr,le`。
- 每行以 `\r\n` 结束，共 11 个逗号分隔项（首项为字母 `P`，其余 10 项为有符号整数）。
- `el=tl-ml`，`er=tr-mr`；`pl/pr` 是包含速度环修正后的最终电机命令。

字段含义：

| 字段 | 含义 |
|---|---|
| `t` | 10 ms 控制周期计数 |
| `tl/tr` | 左右轮目标编码器增量 ×100 |
| `ml/mr` | 左右轮实测编码器增量 ×100 |
| `el/er` | 左右轮速度误差 |
| `pl/pr` | 左右轮最终电机命令 |
| `le` | 循迹误差 |

## 3. 测试记录模板

每次测试记录一行：

| 编号 | 场景 | 参数改动 | 命令/基础速度 | 目标均值 | 实测均值 | 最大误差 | 输出范围 | 现象 | 结论 |
|---|---|---|---:|---:|---:|---:|---|---|---|
| 例 | 架空直行 | 速度环关闭 | 8 | — | L=610/R=590 | — | 8/8 | 方向正常 | 可继续 |

串口监视器应启用时间戳和日志保存。每次测试单独保存文件，例如：

```text
01_link_idle.txt
02_openloop_cmd08_run1.txt
03_openloop_cmd08_run2.txt
04_speed_kp02_ki00.txt
```

## 3.1 改参数、编译和固定命令入口

- 速度环开关、标定值和增益：修改 `chassis_iface.h` 中的 `CHASSIS_SPEED_*` 宏。
- 循迹基础速度和转向限幅：修改 `chassis_iface.c` 顶部的 `CHASSIS_BASE_SPEED`、`CHASSIS_TURN_LIMIT`。
- 循迹 PID：修改 `chassis_iface.c` 中 `pid_init(&line_pid, 24, 0, 10, ...)` 的三个增益。
- 遥测频率和固定电机测试：修改 `main.c` 中的 `PID_TELEMETRY_INTERVAL_TICKS`、`MOTOR_TEST_ALWAYS_RUN`、`MOTOR_TEST_LEFT_SPEED`、`MOTOR_TEST_RIGHT_SPEED`。

固定轮速测试时设置：

```c
#define MOTOR_TEST_ALWAYS_RUN  1
#define MOTOR_TEST_LEFT_SPEED  6
#define MOTOR_TEST_RIGHT_SPEED 6
```

只测试左轮时设为 `6,0`，只测试右轮时设为 `0,6`。当前固定测试持续约 2 秒；20 Hz 遥测时约有 40 帧数据。每次修改后都要重新构建、烧录，再开始下一次测试。调参结束必须恢复：

```c
#define MOTOR_TEST_ALWAYS_RUN 0
```

## 4. 阶段一：验证无线遥测链路

### 操作

1. 牢固架空并固定车体，轮胎前方不得站人；主控和无线模块保持供电，首次链路测试单独断开电机驱动电源。
2. 电脑串口监视器设置为 115200、8 数据位、无校验、1 停止位、无流控。
3. 确认无线发送端与接收端均为透明传输模式。
4. 启动主控，确认能周期性收到标题行和 `P,...` 数据行。
5. 连续接收 2 分钟并保存日志。
6. 检查有无乱码、半行、丢行或连接中断。

### 通过条件

- 连续 2 分钟无乱码。
- 每行字段数量固定。
- `t` 持续增加，没有频繁回零或长时间停顿。
- 当前 2 Hz 配置下，相邻遥测行的 `t` 正常增加约 50；允许偶发丢失一帧，但 2 分钟丢帧率应低于 1%。

### 失败处理

- 全是乱码：逐端核对波特率和 8N1。
- 偶发缺行：检查无线供电、天线、距离和串口模块缓存。
- 主控运行受到打印影响：降低发送频率，不使用阻塞式串口打印。

## 5. 阶段二：提高调试采样率

当前 `PID_TELEMETRY_INTERVAL_TICKS=50`，即每 500 ms 一帧。调参阶段建议临时改为：

```c
#define PID_TELEMETRY_INTERVAL_TICKS 5U
```

这样每 50 ms 一帧，即 20 Hz。它不能显示每一个 10 ms 控制周期，但足以观察持续振荡、超调和稳态偏差，同时不会给 115200 无线串口造成明显压力。

修改后重复阶段一的 2 分钟稳定性测试。20 Hz 时相邻 `t` 应增加约 5。若丢帧率超过 1%，改为 `10U`，即 10 Hz；10 Hz 下仍超过 1%则停止调参，先处理无线链路。

## 6. 阶段三：编码器方向与左右映射

### 操作

1. 架空车轮。
2. 临时设置 `CHASSIS_USE_SPEED_PID=0`，保留编码器计数。
3. 使用低命令值让车辆逻辑前进，建议左右命令均为 6。
4. 观察 `ml` 和 `mr`。
5. 分别只转左轮、只转右轮，确认字段与实际车轮对应。

### 通过条件

- 逻辑前进时 `ml>0` 且 `mr>0`。
- 左轮动作主要改变 `ml`，右轮动作主要改变 `mr`。
- 停止后两侧测量值回到 0 附近。

### 失败处理

- 某侧为负：修改对应 `ENCODER_*_POLARITY`，不要用 PID 参数掩盖方向错误。
- 左右字段互换：检查编码器接线和逻辑轮映射。
- 高速时经常为 0：检查编码器中断、接线和供电，暂不进入闭环调参。

## 7. 阶段四：开环速度标定

### 操作

1. 保持 `CHASSIS_USE_SPEED_PID=0`。
2. 为获得足够稳定段数据，将 `main.c` 固定测试分支中的 `motor_test_ticks < 200` 临时改为 `< 600`，使每档运行约 6 秒。
3. 架空状态分别测试命令 4、6、8、10、12。
4. 每档舍弃前 1 秒，记录后 5 秒的 `ml/mr`。
5. 每档重复 3 次。
6. 对左右轮分别计算稳定段平均值。
7. 计算每个命令点的比例：

```text
比例 = 平均每10ms编码器跳变数 × 100 / 电机命令
```

遥测中的 `ml/mr` 已经乘以 100，因此可直接使用：

```text
比例 = ml或mr的稳定段平均值 / 电机命令
```

8. 去掉电机不能连续转动的死区点；只有接线松动、无线缺帧等已确认故障的数据才作为异常点剔除。把左右轮所有有效比例合并后取中位数，作为新的 `CHASSIS_SPEED_COUNTS_PER_CMD_X100`。
9. 标定完成后把 `< 600` 恢复为 `< 200`。

### 通过条件

- 命令增大时测量速度总体单调增大。
- 同一档三次平均值差异不超过约 10%。
- 左右轮比例差异可以存在，但不应出现方向或数量级错误。

### 注意

该参数描述目标速度模型，不是 PID 增益。标定不准会导致 PI 长期顶着误差工作。架空标定完成后，落地速度可能降低，速度环的积分项负责小幅补偿，不应靠巨大积分修正错误模型。

## 8. 阶段五：速度环只调 P

### 初始设置

```c
CHASSIS_USE_SPEED_PID = 1
KP = 1
KI = 0
KD = 0
```

保持修正限幅为 ±8。

### 操作

1. 架空车轮，左右给相同命令 6。
2. 每次执行一次约 2 秒的固定电机测试并保存日志。
3. 依次尝试 `KP=1、2、3、4...`，一次只增加 1。
4. 每组重复 3 次。
5. 观察 `el/er` 是否快速收敛，以及 `pl/pr` 是否周期性上下摆动。
6. 命令 6 稳定后，再测试命令 8 和 10。

### 判断规则

- 误差长期较大但输出平稳：`KP` 偏小。
- 误差正负交替、输出周期摆动：`KP` 偏大。
- 输出频繁达到“原命令 ±8”：控制已经饱和，先检查目标标定和机械问题。
- 某一侧明显更差：先检查该侧电机、编码器和摩擦，不急于设置不同增益。

### 选值

找到刚开始出现持续振荡的 `KP` 后，退回前一个稳定值；或者取临界值的约 60%～80%。将其作为速度环候选 `KP`。

阶段通过标准：3 次测试中，稳定段平均绝对误差不超过 100（约一个编码器跳变），且修正达到 ±8 限幅的帧数低于 20%，没有连续 5 帧正负交替的误差振荡。

## 9. 阶段六：速度环加入 I

### 初始设置

- 使用阶段五选出的 `KP`。
- `KD=0`。
- `KI` 从 1 开始。

### 操作

1. 架空测试命令 6、8、10。
2. 再落地进行 2～3 米低速直线测试。
3. 依次尝试 `KI=1、2、3...`，每次只增加 1。
4. 观察稳定段 `el/er` 的平均值是否更接近 0。

### 判断规则

- 长期误差减小且没有慢周期摆动：增加有效。
- 车辆每隔一段时间忽快忽慢：`KI` 过大。
- 停车再启动时突然冲击：积分累积或复位路径需要检查。
- 转弯过程中内轮输出不合理地持续增大：积分可能过强。

### 选值

选择能够明显减小稳态误差、但不会产生慢周期振荡的最小 `KI`。速度测量量化明显，通常不加入 D。

阶段通过标准：相比只用 P，稳定段平均误差下降；3 次测试均没有连续 5 帧以上的周期性加速/减速，饱和帧占比仍低于 20%。

## 10. 阶段七：速度环落地验收

依次完成：

1. 低速直行 2 米，3 次。
2. 中速直行 2 米，3 次。
3. 从低速阶跃到中速，观察响应。
4. 左右轮给不同目标，测试缓转弯。
5. 连续运行 30 秒，检查电机和驱动器温升。

### 通过条件

- 直线稳定段左右速度误差均值接近 0。
- 没有持续的快振荡或慢振荡。
- `pl/pr` 大部分时间没有卡在修正限幅。
- 三次测试表现可重复。

其中“接近 0”按稳定段平均绝对误差不超过 100 判断；三次测试的平均速度差异应不超过 10%。

完成后冻结速度环参数，进入循迹调试。

## 11. 阶段八：循迹环只调 P

### 初始设置

- 保持已经调好的速度 PI 不变。
- 循迹 `Ki=0`、`Kd=0`。
- 将基础速度调到安全低速。
- 循迹 `Kp` 建议从 8～12 开始，不直接从当前 24 高速测试。

### 操作

1. 使用一段包含直线和缓弯的简单封闭轨迹。
2. 每个 `Kp` 连续跑 3 圈。
3. 每次只增加 2，例如 10、12、14、16。
4. 保存无线日志，并记录车辆是否压线、摆动、出线。

### 判断规则

- 入弯反应慢、持续偏向一侧：`Kp` 偏小。
- 直线上出现连续蛇形：`Kp` 偏大。
- `le` 已回到中心但车辆仍大幅反向修正：需要 D 抑制，而不是继续增加 P。

选择能可靠过缓弯、直线摆动尚可接受的最小 `Kp`。

## 12. 阶段九：循迹环加入 D

### 操作

1. 保持阶段八的 `Kp`。
2. `Kd` 从 2 开始，每次增加 2。
3. 每组跑相同轨迹 3 圈。
4. 比较 `le` 过零后的反向峰值、直线摆动次数和出弯稳定时间。

### 判断规则

- 摆动幅度和次数下降：D 有效。
- 电机出现短促抖动，`pl/pr` 跳变明显：`Kd` 过大。
- 传感器误差跳变时产生强烈瞬时转向：`Kd` 过大或输入需要滤波。

循迹环通常保持 `Ki=0`。只有确认存在持续单向偏差，并排除电机、传感器安装和速度环差异后，才考虑极小的 I。

## 13. 阶段十：提速与急弯联合优化

1. 每次只把基础速度提高 2 个命令单位。
2. 每档速度完成直线、缓弯、急弯各 3 次。
3. 提速后优先微调循迹 `Kd`，再考虑小幅增加 `Kp`。
4. 不要同时修改基础速度、P、D 和输出限幅。
5. 检查急弯专用速度、丢线恢复和 PID 是否在不同控制分支间正确复位。

达到以下任一条件时停止提速：

- 编码器开始明显漏计。
- 速度环输出长期饱和。
- 无线日志出现主循环异常或大量丢帧。
- 连续三次无法稳定通过相同弯道。
- 电机或驱动器温升异常。

安全硬停止条件：车体固定松动、轮胎或联轴器异常、无线数据中断超过 1 秒、输出连续饱和、驱动器烫手无法持续触碰、车辆进入人员或线缆区域。任一条件出现都应立即切断电机电源，不继续增加参数。

## 14. 每阶段向我反馈的数据

完成一个阶段后，发送以下内容即可继续判断下一步：

```text
阶段：
测试条件：架空/落地，命令或基础速度：
当前参数：标定值、速度KP/KI/KD、循迹KP/KI/KD：
日志：上传串口txt，或粘贴至少30行连续P数据：
实车现象：直行/摆动/冲击/出线/异响：
是否重复：共几次，结果是否一致：
```

不要一次完成全部阶段。首先只执行阶段一；无线链路通过后，把日志发给我，再进入采样率和编码器检查。全部调参结束后，将遥测间隔恢复为适合比赛无线链路的 `50U`，并恢复 `MOTOR_TEST_ALWAYS_RUN=0`。
