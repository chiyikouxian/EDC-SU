# 编码器速度 PID 实车标定

## 当前开关

`chassis_iface.h` 中以下两个开关均已默认开启：

```c
#define CHASSIS_USE_ENCODER   1
#define CHASSIS_USE_SPEED_PID 1
```

速度环每 10 ms 更新一次。循迹层输出左右轮目标命令，速度环使用该命令作为前馈，再根据左右编码器增量分别修正 PWM。

## 串口字段

调试行新增以下字段：

- `E=左累计计数,右累计计数`
- `V=左目标x100,右目标x100/左实测x100,右实测x100`
- `P=左PWM,右PWM`

标定期间可临时把 `main.c` 的 `DEBUG_PRINT_WHILE_RUNNING` 改为 `1`。打印会阻塞主循环，因此只用于低速架空标定，完成后应恢复为 `0`。

## 安全标定步骤

1. 将车轮架空，确保车轮不会接触桌面或线缆。
2. 先保持速度闭环关闭，即临时设置 `CHASSIS_USE_SPEED_PID=0`。
3. 给较低的相同左右命令，确认车轮前进时 `E` 的左右累计计数都增加。任一侧减少时，先修正该侧 `ENCODER_*_POLARITY`，不得开启闭环继续测试。
4. 记录若干档稳定命令下每 10 ms 的实际计数，除以电机命令后设置 `CHASSIS_SPEED_COUNTS_PER_CMD_X100`。例如命令 8 时平均为 6.1 次跳变，则设置为 `6.1 / 8 × 100 ≈ 76`。
5. 恢复 `CHASSIS_USE_SPEED_PID=1`，先设置 `KI=0`，从低速开始逐步调整 `KP`，直到轮速能跟随且不持续振荡。
6. 小幅增加 `KI`，用于消除长期稳态误差；一旦出现周期性忽快忽慢，降低 `KI`。
7. 架空测试通过后再落地低速直行，最后测试循迹和急转弯。

## 回退方式

若出现编码器漏计、方向异常或明显振荡，把 `CHASSIS_USE_SPEED_PID` 设置为 `0`，即可保留编码器计数并恢复原来的开环电机输出。
