# 与硬件 A 的实车联调步骤清单

## 1. 当前联调目标

你这边已经独立确认通过的内容包括：

- 主控状态机正常
- `gray_sensor -> line_track -> track_bridge` 数据链正常
- `B=` 在 `RUNNING` 时 fresh、非 `RUNNING` 时 stale
- `DRV / NAV / ADV` 的主控逻辑层可正常进入 `RUN`
- `chassis_iface.c` 已能消费 bridge，`C=` 状态会按新逻辑变化

因此下一阶段不再重复验证“逻辑是否接通”，而是进入：

**真实电机驱动 + 底盘动作执行 + 整车联调**

当前联调核心问题只有一个：

> bridge 数据已经能流到底盘逻辑，但真实电机执行层是否真的被驱动起来。

---

## 2. 联调顺序总览

建议严格按这个顺序进行：

1. 电机底层动作验证
2. 刹车 / 停车动作验证
3. 原地左旋 / 右旋验证
4. bridge 驱动真实底盘动作验证
5. DRV 真实循迹联调
6. 丢线 / 找线联调
7. 节点阈值联调
8. NAV 联调
9. ADV 联调

不要一上来直接跑整张地图。

---

## 3. 联调前必须确认的事项

在开始前，和硬件 A 先现场确认：

- `CHASSIS_USE_TRACK_BRIDGE == 1`
- `CHASSIS_TRACK_ERROR_SCALE` 当前值是多少
- `CHASSIS_LOST_LINE_SHORT_TICKS` 当前值是多少
- `CHASSIS_LOST_LINE_LONG_TICKS` 当前值是多少
- `CHASSIS_NODE_ACTIVE_COUNT_THRESHOLD` 当前值是多少
- `CHASSIS_USE_ENCODER == 0`

并确认硬件 A 已经把以下函数接入真实电机驱动：

- `chassis_drive()`
- `chassis_brake()`
- `chassis_rotate_left()`
- `chassis_rotate_right()`

如果这些还是空函数，那就还不能进入实车联调。

---

## 4. 第一阶段：真实电机底层动作验证

### 4.1 前进动作

目标：
- 验证 `chassis_drive()` / `motor_set_speed()` 已真正输出到底盘

操作：
- 让硬件 A 准备最小测试代码或最小调试入口
- 仅测试固定前进，不接循迹

预期：
- 小车能稳定前进
- 左右轮方向一致
- 不出现单轮不转或反转

### 4.2 刹车动作

目标：
- 验证 `chassis_brake()` 真实生效

操作：
- 前进后立即执行刹车

预期：
- 小车立即停下
- 行为与硬件 A 设计一致

### 4.3 左旋 / 右旋动作

目标：
- 验证找线动作基础能力成立

操作：
- 分别触发左旋和右旋

预期：
- 左旋与右旋方向正确
- 不出现方向相反

---

## 5. 第二阶段：bridge 驱动真实底盘动作

### 5.1 bridge 数据进入底盘控制

目标：
- 验证底盘实际在消费 `track_bridge_get()`

操作：
- 进入 `DRV -> RUN`
- 手动改变黑线位置：左、中、右
- 观察串口 `L=` / `B=` / `C=`
- 同时观察真实底盘动作趋势

预期：
- `B=` 随黑线位置变化
- 底盘动作方向与 `error` 符号一致
- 线偏左时车应向左修正
- 线偏右时车应向右修正

### 5.2 stale 保护动作

目标：
- 验证 stale 会触发真实停车保护

操作：
- 让系统离开 `RUNNING`
- 或进入 stale 场景

预期：
- `B=STALE`
- 真实底盘立即停车

---

## 6. 第三阶段：DRV 真实循迹联调

### 6.1 最小黑线循迹

目标：
- 验证 DRV 模式真实跑线

操作：
- 切到 `DRV`
- 长按启动
- 将车放在标准黑线上

预期：
- `SBEEP -> RUN`
- `C=FLIN/FOLL`
- 车沿线前进
- 不是只改串口状态而车不动

### 6.2 误差方向验证

目标：
- 验证 `error` 与真实转向一致

操作：
- 让线偏左、偏右

预期：
- `error < 0` 时车向左修正
- `error > 0` 时车向右修正

如果方向相反：
- 优先检查硬件 A 电机左右轮方向定义
- 再检查 `forward/turn -> left/right wheel speed` 混合逻辑

---

## 7. 第四阶段：丢线 / 找线联调

### 7.1 短时丢线

目标：
- 验证短时丢线后会原地找线

操作：
- 让车先正常跑线
- 短暂偏离黑线
- 再重新回到黑线

预期：
- 进入找线动作
- 找线方向符合 `last_line_error` 规则
- 重新看到线后退出找线并继续循迹

### 7.2 长时丢线

目标：
- 验证长时丢线会停车保护

操作：
- 持续脱离黑线超过长时阈值

预期：
- 车停止
- 状态进入 `LINE_LOST` / 主控后续错误保护流程

---

## 8. 第五阶段：节点阈值联调

### 8.1 节点识别

目标：
- 验证 `active_count >= CHASSIS_NODE_ACTIVE_COUNT_THRESHOLD` 的实车效果

操作：
- 经过较宽黑线或交叉区域
- 观察节点触发情况

预期：
- 节点触发合理
- 不漏判
- 不会在一个节点内疯狂连加

### 8.2 阈值微调

如果出现：

- 太敏感：经常误判节点
- 太迟钝：节点不触发

则微调：

```c
#define CHASSIS_NODE_ACTIVE_COUNT_THRESHOLD 6
```

建议一次只改 1。

---

## 9. 第六阶段：NAV 联调

### 9.1 NAV 整链路联调

目标：
- 验证视觉识别 + bridge + 真实底盘循迹

操作：
- 切到 `NAV`
- 长按启动
- 发送目标帧，例如：

```text
$RECT,C,1\r\n
```

预期：
- `FDET -> SBEEP -> RUN`
- `T=C`
- `B=` fresh
- 底盘真实开始循迹

---

## 10. 第七阶段：ADV 联调

### 10.1 ADV 基础联调

目标：
- 验证 ADV 模式进入 `RUNNING` 后，真实底盘同样能消费 bridge

操作：
- 切到 `ADV`
- 长按启动
- 发送目标帧，例如：

```text
$CIRCLE,A,1\r\n
```

预期：
- `GSCAN -> SBEEP -> RUN`
- `T=A`
- `B=` fresh
- 真实底盘开始循迹

注意：
- 当前这里仍是“底盘联调”
- 云台真实扫描闭环是否已经接入，取决于视觉/云台同学的进度

---

## 11. 联调时重点观察的串口字段

联调时请重点盯：

- `S=`：主控状态是否按预期推进
- `M=`：模式是否正确
- `T=`：目标点是否正确
- `C=`：底盘状态是否合理
- `L=`：实时误差
- `B=`：bridge 锁存误差

判断原则：

- `L=` / `B=` 趋势一致即可
- `B=` 是底盘正式输入
- `C=` 反映的是底盘控制逻辑状态

---

## 12. 每轮联调建议记录的表

建议每一项记录：

- 测试项
- 代码版本 / commit
- 参数宏值
- 操作步骤
- 串口现象
- 实车动作
- 是否通过
- 备注

---

## 13. 当前联调优先级建议

建议按这个优先级执行：

1. 电机前进 / 刹车 / 左旋 / 右旋
2. DRV 真实循迹
3. 丢线 / 找线
4. 节点识别
5. NAV
6. ADV

只有 `DRV` 真实循迹跑通以后，再继续 `NAV/ADV` 才有效率。
